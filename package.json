{
  "name": "slatedb",
  "version": "0.2.0",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "build": "cargo build --release && rm -f slatedb_napi.darwin-arm64.node && cp target/release/libslatedb_napi.dylib slatedb_napi.darwin-arm64.node && codesign -s - slatedb_napi.darwin-arm64.node",
    "test": "bun test",
    "bench": "bun run bench.ts",
    "bencher": "bun run bencher.ts",
    "bencher:db": "bun run bencher.ts db",
    "bencher:txn": "bun run bencher.ts transaction"
  }
}
